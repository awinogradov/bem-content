block('code')(

    tag()('pre'),

    js()(true),

    content()(function () {
        return [
            {
                elem: 'source',
                tag: 'code',
                cls: this.ctx.mods.lang,
                content: this.ctx.content
            }
        ];
    })
)
